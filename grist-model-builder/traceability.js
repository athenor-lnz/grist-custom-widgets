(() => {
  'use strict';
  // Depuis la v1.1.3, la traçabilité, les modales et le cycle de construction
  // sont gérés directement par app.js. Ce fichier est conservé temporairement
  // pour compatibilité avec les pages éventuellement encore en cache.
})();
